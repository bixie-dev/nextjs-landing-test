import '../styles/Login.module.css';

const InputText = (props: { placeholder: string }) => (
  <>
    <input className="" placeholder={props.placeholder} type="text" />
  </>
);

export { InputText };
